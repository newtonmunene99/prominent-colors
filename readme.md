# Color Palette

Uses node-vibrant to extract prominent colors.

## Getting Started

run:

```bash
npm i
```

then:

```bash
npm start
```

To build the app for production, run:

```bash
npm run build
```

T
